// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("peblog", "for peblog support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("peblog", "enable peblog support", "no");

if (PHP_PEBLOG != "no") {
	EXTENSION("peblog", "peblog.c");
}

